# ViDSPy Environment Variables
# Copy this file to .env and fill in your actual credentials
#
# For non-sensitive configuration (optimizer settings, cache directories, etc.),
# use vidspy_config.yaml instead. Generate a template with:
#   python -c "from vidspy.setup import create_example_config; create_example_config()"

# ==============================================================================
# API Keys and Tokens (REQUIRED)
# ==============================================================================

# OpenRouter API Key (required for cloud-based VLM)
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here

# OpenRouter API Key for DSPy Optimizers (required for MIPROv2, COPRO, GEPA)
# Used by optimizers to generate instruction variations
# If not set, will use OPENROUTER_API_KEY as fallback
OPENROUTER_OPTIMIZER_API_KEY=your-openrouter-api-key-here

# HuggingFace Token (optional, only needed for private/gated models)
# Get your token from: https://huggingface.co/settings/tokens
# HUGGINGFACE_TOKEN=your-huggingface-token-here
